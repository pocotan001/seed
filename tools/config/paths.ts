import * as path from "path";

export const ROOT_DIR = path.join(__dirname, "../..");
export const SRC_DIR = `${ROOT_DIR}/src`;
export const DIST_DIR = `${ROOT_DIR}/dist`;
export const SCRIPTS_DIR = `${ROOT_DIR}/tools/scripts`;
