import { LoginResponse } from "../../../../client/infra/repositories/AuthRepository";

const resp: LoginResponse = {
  token: "abracadabra",
  me: {
    id: 0,
    name: "pocotan001"
  }
};

export default resp;
