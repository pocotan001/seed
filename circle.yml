general:
    branches:
        ignore:
            - gh-pages

machine:
    timezone: Asia/Tokyo

deployment:
    ghpages:
        branch: master
        commands:
            - git config --global user.name 'circleci'
            - git config --global user.email 'circleci@example.com'
            - npm run ghpages
