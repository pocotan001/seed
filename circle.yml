general:
    branches:
        ignore:
            - gh-pages

machine:
    timezone: Asia/Tokyo

deployment:
    styleguide:
        branch: master
        commands:
            - git config --global user.name 'circle'
            - git config --global user.email 'circle@example.com'
            - npm run ghpages
