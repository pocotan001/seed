general:
    branches:
        ignore:
            - gh-pages

machine:
    timezone: Asia/Tokyo

deployment:
    styleguide:
        branch: master
        commands:
            - git config user.name 'circle'
            - git config user.email 'circle@example.com'
            - npm run ghpages
