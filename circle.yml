general:
    branches:
        ignore:
            - gh-pages

machine:
    timezone: Asia/Tokyo
    node:
        version: 0.12.0

deployment:
    styleguide:
        branch: master
        commands:
            - npm run ghpages
